<?php

/**
 * Function that adds a menu item to access the list of cupcake
 * @return mixed
 */
function cupcake_menu(){
    $items['cupcake/list'] = array(
        'page callback' => 'list_cupcake',
        'access callback' => TRUE,
        'title' => 'List Cupcake',
        'weight' => 0
    );
    return $items;
}

function list_cupcake(){
    return "listado-...";
}